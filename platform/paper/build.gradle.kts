dependencies {
    compileOnly("io.papermc.paper:paper-api:1.21.4-R0.1-SNAPSHOT") // .4 is used for the registerCommand method. The rest is exactly the same.
    implementation(project(":platform:common"))
}